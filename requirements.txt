# Python runtime is managed outside pip (recommended: Python 3.10)

# Core numerical/data stack
numpy<2.0
pandas>=2.1
pillow>=10.0,<12

# PyTorch stack (CPU by default via pip; install CUDA wheels separately if needed)
torch>=2.4,<2.8
torchvision>=0.19,<0.23
torchaudio>=2.4,<2.8

# Diffusion / Transformers / VLM
diffusers==0.35.1
transformers==4.57.1
accelerate>=1.0,<2
qwen-vl-utils[decord]>=0.0.10
huggingface_hub>=0.28
einops>=0.8.0
safetensors>=0.4.5
sentencepiece>=0.2.0
protobuf>=4.25

# Image / video / classifier utilities
opencv-python-headless>=4.10
onnxruntime>=1.16,<1.19
pydload>=1.0.9
scikit-image>=0.21
progressbar2>=4.4

# Logging / experiment tracking
tqdm>=4.66
wandb>=0.19
